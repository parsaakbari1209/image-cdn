{
  "scripts": {
    "dev": "wrangler dev src/index.ts --local --persist --host 127.0.0.1 --port 8080",
    "deploy": "wrangler publish src/index.ts"
  },
  "dependencies": {
    "hono": "^3.0.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20230228.0",
    "@miniflare/tre": "^3.0.0-next.12",
    "miniflare": "^2.12.1",
    "wrangler": "^2.7.1"
  }
}
